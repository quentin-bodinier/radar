# Sensitive / generated data
today.html
team.html
state/*
# but ship the example templates teammates need to bootstrap
!state/config.example.json
!state/commitments.example.json

# Local Claude settings
.claude/settings.local.json

# macOS
.DS_Store
